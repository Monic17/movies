// const controller = require('../../src/api/movie.service');
// const { videoDataFactory1 } = require('../factories/movie.factory');

describe('invoice', () => {
  test('should try to find an outgoinginvoice by its document_id', async () => {});
});
